{
  "name": "carewell-firestore-rules-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test:rules": "firebase emulators:exec --only firestore --project demo-carewell 'vitest run'"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.7.1",
    "firebase-tools": "^13.29.1",
    "vitest": "^1.6.0"
  }
}
